const baseURL = "https://cfgxbabec.onrender.com";

const recordEndpoint = `${baseURL}/records`;
const studentEndpoint = `${baseURL}/students`;
const teacherEndpoint = `${baseURL}/teachers`;
const orgEndpoint = `${baseURL}/orgs`;
const orderEndpoint = `${baseURL}/orders`;

export {
  recordEndpoint,
  studentEndpoint,
  teacherEndpoint,
  orgEndpoint,
  orderEndpoint,
};
